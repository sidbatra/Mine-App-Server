
<div id="container" class="container well">
  <legend>Create a new style</legend>

  <%form_for @style, :url => "/admin/styles" do |f|%>
    <%=render :partial  => "input",
              :locals   => {:f => f,:submit_text => "Create"}%>
  <%end%>

</div>

<%content_for :javascript do%>
  <script type="text/javascript">
    new Denwen.Views.Admin.Styles.New({
          el            : $('#container'),
          uploadConfig  : <%=@upload_config.to_json%>});
  </script>
<%end%>
