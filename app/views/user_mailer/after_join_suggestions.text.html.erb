                    <%content_for :header do%>
                      <%=render :partial => "header_image",
                          :locals => {
                            :url => welcome_url(WelcomeFilter::Create,:suggestion_id => @suggestion.id,:src => @source),
                            :image => "header-image-add-#{@suggestion.thing.gsub(' ','-')}.jpg",
                            :alt => "Mine: "}%>
                    <%end%>

                    <%=render :partial => "action_button",
                        :locals => {
                          :url => welcome_url(WelcomeFilter::Create,:suggestion_id => @suggestion.id,:src => @source),
                          :text => "Add your first #{@suggestion.thing} on "\
                                    "<font face='Arial' "\
                                    "style='font-style: normal;'> "\
                                    "<b>#{CONFIG[:name].downcase}</b>"}%>
