Safety check when a user is deleted.   <br/><br/>

<a href="<%=user_url(@user_deleted.handle)%>">
  <%=@user_deleted.first_name + " " + @user_deleted.last_name%>
</a>

<br/><br/>

Ignore if it's a safe deletion.

