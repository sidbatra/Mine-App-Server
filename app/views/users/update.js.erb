<%=render :partial => "response/js",
    :locals => { 
      :success          => !@error,
      :success_message  => CONFIG[:success],
      :error_message    => CONFIG[:error],
      :body             => { :byline => h(self.current_user.byline)}
      }-%>






