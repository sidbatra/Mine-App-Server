<%-cache @key do-%>
  <%=@comments.to_json(
      :only => [:id,:data,:created_at],
      :include => {
        :user => {
          :only => [:id,:first_name,:last_name,:handle],
          :methods  => [:photo_url]}})-%>
<%-end-%>
