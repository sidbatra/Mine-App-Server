<%-cache @key, :expires_in => 4.hours do-%>
  <%={
      :title => @title,
      :description => @description,
      :query => @query,
      :sane_query => @sane_query,
      :page => @page,
      :products => @products}.to_json-%>
<%-end-%>
