<%-@product[:html] = render(
                      :partial => "products/display.html.erb",
                      :locals => {
                        :product => @product}) if params[:product][:render]-%>
<%=@product.to_json(:only => [:id,:html])-%>
