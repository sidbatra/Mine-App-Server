<%=object.to_json(
    :only => [:id,:created_at,:title,:handle,:endorsement,:source_url],
    :methods => [:giant_url,:fb_object_id],
    :include => {
      :user => {
        :only => [:id,:first_name,:last_name,:gender,:handle],
        :methods => [:square_image_url]},
      :store => {
        :only => [:id,:is_processed,:name],
        :methods => [:thumbnail_url]}})%>
