<%form_for @collection,
	      :html => {:class => '', :style => ''} do |f|%>

  <%=render :partial  => 'input',
            :locals   => {:f => f}%>

<%end%>

<%content_for :javascript do%>
  <script type="text/javascript">

    new Denwen.Views.Collections.New({
            currentUserJSON : <%=self.current_user.to_json(
                                  :only => [:id,:first_name,:last_name,
                                              :handle])%>,
                     source : '<%=@source%>',
                         el : $('#container')});

  </script>
<%end%>

