# Apache settings for multiple rails application
# running on the server

NameVirtualHost *:80

  PassengerPoolIdleTime 0
  PassengerMaxPoolSize 6

<VirtualHost *:80>

  # Admin email, Server Name (domain name) and any aliases
  ServerName  felvy.com

  # Index file and Document Root (where the public files are located)
  DocumentRoot /vol/felvy/current/public
  RailsEnv production
  PassengerMinInstances 3

  <Directory /vol/felvy/current/public>
       AllowOverride all      
       Options -MultiViews

    <FilesMatch "\.(ico|eot|svg|ttf|woff)$">
       FileETag none
    </FilesMatch>
  </Directory>

  <ifModule mod_expires.c>
    ExpiresActive On
    ExpiresDefault A0

    <FilesMatch "\.(ico|eot|svg|ttf|woff)$">
     ExpiresDefault "access plus 1 year"
    </FilesMatch>
  </ifModule>

  # Custom log file productions
  LogLevel warn
  ErrorLog  /vol/felvy/current/log/error.log
  CustomLog /vol/felvy/current/log/access.log combined

</VirtualHost>


<VirtualHost *:443>

  # Admin email, Server Name (domain name) and any aliases
  ServerName  felvy.com

  # Index file and Document Root (where the public files are located)
  DocumentRoot /vol/felvy/current/public
  RailsEnv production
  PassengerMinInstances 3

  <Directory /vol/felvy/current/public>
       AllowOverride all      
       Options -MultiViews
  </Directory>

  # Custom log file productions
  LogLevel warn
  ErrorLog  /vol/felvy/current/log/error.log
  CustomLog /vol/felvy/current/log/access.log combined
  SSLEngine on
  SSLCertificateFile /vol/felvy/current/private/ssl/felvy.com.crt
  SSLCertificateKeyFile /vol/felvy/current/private/ssl/felvy.com.key
  SSLCertificateChainFile /vol/felvy/current/private/ssl/gd_bundle.crt
</VirtualHost>


